<Page
    x:Class="NBPClient.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NBPClient"
    xmlns:local2="using:NBPClient.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


        <ListView ItemClick="ListView_ItemClick_1"  ItemsSource="{x:Bind ViewModel.Currencies}"
    HorizontalAlignment="Center" VerticalAlignment="Center">
            <ListView.ItemTemplate>
                <DataTemplate  x:DataType="local2:CurrencyModel">
                    <StackPanel   Orientation="Horizontal" Margin="6">
                        <SymbolIcon Symbol="Audio" Margin="0,0,12,0"/>
                        <StackPanel>
                            <TextBlock Text="{x:Bind Currency}" FontWeight="Bold"/>
                            <TextBlock Text="{x:Bind Mid}"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <CalendarDatePicker DateChanged="CalendarDatePicker_DateChanged" ></CalendarDatePicker>
    </Grid>
</Page>
